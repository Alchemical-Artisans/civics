node_modules

# Scratch space. Nothing in the project writes here any more -- it held the
# downloaded PDF corpus back when documents were converted automatically -- but
# the rule stays so an old checkout does not suddenly offer 2 GB to commit.
/.cache/

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
# Paraglide
src/lib/paraglide
project.inlang/cache/

*storybook.log
storybook-static
docs/.obsidian
