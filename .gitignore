node_modules

# Documents downloaded by the calendar scripts. Only the converted HTML in
# src/lib/data/documents/ is committed; these are kept so re-running the
# conversion does not re-download the corpus. Safe to delete.
/.cache/

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
# Paraglide
src/lib/paraglide
project.inlang/cache/

*storybook.log
storybook-static
docs/.obsidian
